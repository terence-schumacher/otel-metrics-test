apiVersion: v1
kind: Service
metadata:
  name: {{ include "otel-metrics-test.fullname" . }}
  labels:
    {{- include "otel-metrics-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "otel-metrics-test.selectorLabels" . | nindent 4 }}
